i want my current project to be like claude code where its more flexible. reference to reference-projects/claude-code-reverse

update: instead of modifying the existing code, archive the current `src/` and implement a brand-new `src/` that follows the claude-like requirements.
